import { useSaved, useStudios } from '../product/data';
import { Empty, Eyebrow, ExperienceCard, Grid, Lead, LoadingCards, Notice, Page, Wrap } from '../product/ui';
export default function Saved(){const{saved}=useSaved();const{studios,loading,error}=useStudios();const items=studios.filter(s=>saved.includes(s.id));return <Page><Wrap><Eyebrow>Your little collection</Eyebrow><h1>Keep the ones<br/>that speak to you.</h1><Lead>A shortlist for your Seoul trip. Saved on this device, ready when you are.</Lead>{error?<Notice role="alert">{error}</Notice>:loading?<LoadingCards/>:items.length?<Grid>{items.map(s=><ExperienceCard key={s.id} studio={s}/>)}</Grid>:<Empty title="A little inspiration goes a long way." body="Tap the heart on any experience to keep it here. You don’t need an account to start a collection."/>}</Wrap></Page>;}
